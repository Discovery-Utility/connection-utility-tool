module.exports = {
    DEMO_MODE: true,
    SHOW_DEV_CONSOLE: true,
    SEARCH_DELAY: 2,        //seconds to search appliances, default = 60sec
    //PATH_TO_CERT_EXE: "\"" + __dirname.split("app.asar")[0] + "app.asar.unpacked\\src\\cert.exe\" " // switch than build
    PATH_TO_CERT_EXE: "\"" + __dirname + "\\cert.exe\" "
};
